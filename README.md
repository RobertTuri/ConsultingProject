Need to finish coding the scraping of articles, the AI selected for sentiment analysis and then the actual dashboard design. Bonus could be to scrape a finviz-like website for a stock showing...

In order to install all packages follow the below list:
1. In your virtual environment use the command: pip install -r requirements.txt
2. After all packages are installed, run setup.py 
3.
